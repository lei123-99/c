名称：ZB

在：
  workflow_dispatch：  
      分支：
        - 主要的
工作：
  建造：
    运行：ubuntu-latest

    步骤：
      - 名称：结帐代码
        用途：Action/Checkout@V2

      - 名称：设置python
        用途：action/setup-python@v2
        和：
          python-version：3.x          

      - 名称：安装依赖项
        运行：pip安装硒请求期货eventlet

      - 名称：运行ipjd
        运行：python $ {      { github.workspace }      } /ipjd1.py
    
      -名称：提交更改
        运行：|
          git config-local user.email“ahua@github.com”
          git config -local user.name“ahua”
          如果[ -n“ $（git- percerain）”;然后
            git提交-am“自动更新”
            git推
            别的
              回声“未检测到的变化，跳过提交。”
            fi
